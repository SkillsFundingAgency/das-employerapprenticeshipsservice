
@model SFA.DAS.EAS.Web.Models.ConfirmNewPayeScheme
@{ViewBag.Title = "Search for your company";}
<div class="grid-row">
    <div class="column-two-thirds">
        <h1 class="heading-xlarge">Enter your Companies House number</h1>
        <p>Use the Companies House number for an organisation that your apprentices will be employed through.</p>
        <p><a href="https://beta.companieshouse.gov.uk/search/" rel="external" target="_blank">Find my Companies House number</a></p>
        <form method="POST" action="@Url.Action("SelectCompany")">
            @Html.AntiForgeryToken()
            <div class="form-group @(TempData["companyNumberError"] != null ? "error" : "")">
                <label class="form-label" for="EmployerRef">Companies House number</label>

                @if (TempData["companyNumberError"] != null)
                {
                    <span class="error-message" id="error-message-companies-house">Enter a valid Companies House number</span>
                }
                
                <input class="form-control form-control-3-4" id="EmployerRef" name="LegalEntityCode" type="text"
                       aria-required="true"/>
            </div>
            <input type="hidden" value="@Model.PayeScheme" name="PayeScheme"/>
            <input type="hidden" name="HashedId" value="@Model.HashedId" />
            <input type="hidden" value="@Model.RefreshToken" name="RefreshToken"/>
            <input type="hidden" value="@Model.AccessToken" name="AccessToken"/>
            <button type="submit" class="button" id="continue">Continue</button>
        </form>
    </div>
</div>

@section breadcrumb {
    <div class="breadcrumbs">
        <ol role="navigation">
            <li><a href="@Url.Action("Index", "EmployerAccountPaye", new { accountId = Model.HashedId })" class="back-link">Back to PAYE schemes</a></li>
        </ol>
    </div>
}
