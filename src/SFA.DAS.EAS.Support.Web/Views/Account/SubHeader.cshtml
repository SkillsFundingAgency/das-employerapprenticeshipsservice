@model SFA.DAS.EAS.Support.Core.Models.Account
@{
    Layout = null;
}

<div class="grid-row">
    <div class="column-full">
        <h1 class="heading-large">
            @(Model?.DasAccountName)
            @if (!string.IsNullOrEmpty(Model?.HashedAccountId))
            {
                <span class="heading-secondary"> Account ID @(Model?.HashedAccountId), created @Model.DateRegistered.ToString("dd/MM/yyyy")</span>
            }
        </h1>
    </div>
</div>
