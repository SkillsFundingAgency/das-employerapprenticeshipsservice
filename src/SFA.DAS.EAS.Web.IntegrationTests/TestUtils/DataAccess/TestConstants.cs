using System;

namespace SFA.DAS.EAS.Account.API.IntegrationTests.TestUtils.DataAccess
{
    static class TestConstants
    {
        public const int UserId = 1;
        public static TimeSpan DbTimeout = new TimeSpan(0, 1, 0);
    }
}